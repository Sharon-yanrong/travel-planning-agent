# 旅行规划 Agent · 产品需求文档（PRD）

| 版本 v1.0 · 作者 周艳蓉 · 2026-09 · MVP 已验证并上线，规划 V1 |
|---|

## 1. 背景与问题

自由行用户在行程规划中面临三个突出问题：**信息分散**（攻略、天气、景点、价格分布于多个平台，需反复切换与整合）；**缺乏个性化**（现有攻略面向大众，难以回应「单人 / 预算有限 / 时间有限」等具体约束）；**结果不可执行**（多为信息罗列，而非可直接照做的日程）。带孩子等有明确约束的用户问题更为明显——还需额外考虑孩子体力、雨天备选、免费选项等。

大模型带来的机会在于：将「用户自行检索、整合」转变为「通过对话获得贴合自身约束、可直接执行的行程」，即从**信息检索**升级为**辅助决策**。

## 2. 目标与非目标

**目标（本版本）**：用户以自然语言描述需求，即可获得具体、可执行、并附有编排理由的行程；系统需结合实时天气与真实景点数据，并记忆用户偏好，避免重复询问。

**非目标（本版本明确不做，以控制范围）**：
- 不接入真实机票 / 酒店 / 门票的预订与支付（列入 V2）；
- 不追求全球全量景点，优先覆盖少数城市；
- 不支持多人协同规划。

## 3. 目标用户与场景

**核心用户**：带孩子自由行的家长。典型画像为一位 7 岁孩子的家长，预算敏感、英语一般、时间有限，重视避免排队、控制孩子疲劳、降低踩雷风险。选择该人群的原因：其约束最多、现有工具适配最差，产品价值最易被感知。

**用户故事**：
- 作为用户，我希望一句话说清需求，无需自行翻阅大量攻略；
- 作为预算敏感用户，我希望优先看到免费或高性价比的选项；
- 作为多次使用者，我希望系统记住我的情况（如带孩子、不偏好博物馆），无需重复说明。

## 4. 竞品与机会

现有方案大致分三类：**内容攻略**（小红书、马蜂窝等）内容丰富但静态、不适配个人约束，仍需用户自行整合；**搜索与地图**擅长单点查询，但不负责整日编排；**OTA 的 AI 助手**（如携程）方向一致且具备数据优势。

差异化机会在于三点：**更懂约束**（带娃、预算等硬条件被真正纳入）、**更可执行**（提供带理由的分日程而非信息堆砌）、**具备记忆**（越用越省心）。

## 5. 功能需求（按优先级）

**P0 —— MVP，已在原型中验证：**
- **F1 自然语言需求理解与多轮对话**：用户一句话进入，可持续澄清与调整。
- **F2 实时天气（工具）**：涉及户外活动时自动查询天气并据此调整（如遇雨改为室内为主）。
- **F3 景点检索（工具 + 数据）**：按「亲子 / 公园 / 预算」等条件筛选，返回名称、简介与大致花费。
- **F4 攻略知识增强（RAG）**：将在地实用信息（携带雨具、交通卡购买、单日不宜过满等）融入建议，并可溯源。
- **F5 偏好记忆**：记录用户透露的偏好并在后续自动规避（如已说明不偏好博物馆，则不再推荐）。
- **F6 结构化行程与理由**：输出按天的行程，每项附「为什么这样安排」的说明。

验收标准示例（F2）：当用户提及某城市且行程含户外活动时，系统须调用天气工具，并在最终建议中体现「雨天转室内」的调整，避免只查询而不应用。

**P1 —— 增强**：行程时间轴 + 地图可视化；补充平价餐饮与交通建议；支持手动编辑行程。
**P2 —— 闭环与规模化**：接入真实机票 / 酒店 / 门票的查询与预订；将记忆升级为结构化用户画像以支持个性化；扩展城市与多语言。

## 6. 核心流程

```
用户输入需求
  → 应用加载[记忆] 并检索[攻略(RAG)]           （应用自动执行，模型不感知此步）
  → 大模型结合 需求 + 记忆 + 攻略 进行判断
  → 模型按需请求调用工具（天气 / 景点 / 记忆）    （模型驱动，按需触发）
  → 应用执行工具并将结果回传模型
  → 模型生成「带理由的分日行程」
  → 用户追问与调整（回到多轮对话）
```

关键设计：**攻略经验走 RAG（应用每轮自动注入），景点与天气走工具（模型按需主动调用）**。攻略类知识相对稳定，适合预先检索；天气、价格等高频变化数据只能实时获取。

## 7. 数据与指标

- **北极星指标**：规划完成率——用户从提问到最终接受一份行程的比例，最能反映产品是否真正有效。
- **关键指标（AARRR）**：首次对话完成率（激活）；平均对话轮数、工具调用成功率（参与）；行程采纳率、建议认可度（价值）；7 日回访率（留存）。
- **采集方式**：记录每轮调用的工具及参数、建议是否被采纳、追问次数等。

## 8. AI 产品的特有考量

除常规产品要素外，AI 产品还需重点处理以下问题：
- **能力边界与降级**：高频变化数据（价格、余量）须通过工具实时获取，不纳入 RAG（易过期）；模型本身无法执行动作，预订、发送等操作只能交由工具完成。
- **控制幻觉**：开放时间、价格等关键事实以工具 / RAG 数据为准，明确要求模型「无数据不臆造」，并以评测集（eval）做回归，避免改动引入新问题。
- **避免厂商锁定**：模型调用统一采用 OpenAI 兼容接口进行抽象，更换厂商仅需修改配置。开发中曾遇到相关问题——原用的免费模型服务中途下线（接口返回 410），因已提前建立抽象层，切换至其他厂商仅修改配置、未改动代码，数分钟内恢复，进一步验证了厂商抽象与降级预案的必要性。
- **成本**：优先使用小模型并配合提示词缓存，同时监控每次会话的 token 消耗。
- **隐私**：用户偏好本地 / 加密存储，遵循最小化收集原则。

## 9. 里程碑

- **M0 · MVP（已完成）**：P0 全部功能，单城市，网页原型已跑通并部署上线。
- **M1**：结构化行程 + 地图，覆盖 3–5 个城市，通过一轮内测采集上述指标。
- **M2**：接入预订闭环，建立结构化用户画像，扩展规模与多语言。

## 10. 风险与应对

- 模型能力与价格变动、厂商可能停服 → 多厂商抽象 + 成本监控（已验证可行）。
- 景点 / 攻略数据过期 → 建立内容更新机制，并使检索结果可溯源。
- 幻觉导致错误建议 → 关键事实以工具数据为准，配合 eval 回归。

---

*角色说明：需求定义、架构设计（工具调用 + RAG + 记忆）、开发与验证均由本人独立完成。*
