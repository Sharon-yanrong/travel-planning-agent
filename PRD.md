# 旅行规划 Agent · 产品需求文档（PRD）

| 版本 | v1.0（草案） · 作者 周艳蓉 · 2026-09-13 · MVP 已验证，规划 V1 |
|---|---|

## 1. 我为什么想做这个

去年我一个人做欧洲多国自由行，真正累人的不是走路，是**规划**。攻略在小红书、天气在天气 App、景点在地图、价格在 OTA——我得开五个窗口来回拼；更麻烦的是，所有攻略都是写给"所有人"的，没人告诉我"你一个人、预算有限、只有三天，到底该怎么排"。后来帮带孩子的朋友出主意，发现她的约束更多：孩子会不会累、下雨去哪、有没有免费的地方。

大模型让我看到另一个解法：**用户不用自己搜，而是边聊边拿到一份"懂你情况、能直接照做"的行程**。这就是这个产品想解决的问题——把"信息检索"变成"帮你做决定"。

## 2. 目标与非目标

这一版我只想把一件事做到位：**用户用大白话说清需求，就能得到一份具体、能照着走、并且说得清"为什么这么排"的行程**；同时它要能结合实时天气和真实景点数据，还得记住用户的偏好，别每次都从头问。

明确**不做**的（这一版划清边界，免得越做越大）：
- 不碰真实的机票/酒店/门票预订和支付——那是另一套系统，放到 V2；
- 不追求全球全量景点，先把少数几个城市做好；
- 不做多人协同规划。

## 3. 目标用户与场景

**核心用户：带孩子自由行的家长。** 典型画像是一位 7 岁孩子的妈妈，预算敏感、英语一般、时间紧，最怕排队、怕孩子闹、怕踩雷。我选这个人群，是因为他们的约束最多、现有工具最不好用，价值最容易被感知。

他们真正想要的是这几件事（用户故事）：
- "我想一句话说清需求，别让我自己翻十篇攻略。"
- "免费和高性价比的地方先给我看，钱要花在刀刃上。"
- "它最好记得我带着孩子、不爱逛博物馆，别让我每次重复。"

## 4. 竞品与机会

现在解决这个问题的大致三类玩家：内容攻略（小红书、马蜂窝）内容多但静态、不懂你的具体情况，还是得自己拼；搜索和地图能查单个点，但不负责把一整天串起来；携程这类 OTA 的 AI 助手方向和我一致，也有数据优势。

我认为差异化的空间在三点：**更懂约束**（带娃、预算这些硬条件真的被用上）、**更可执行**（给到带理由的分日程，不是一堆信息）、**有记忆**（越用越省事）。

## 5. 功能需求（按优先级）

**P0——MVP，已在原型里跑通：**
- **F1 自然语言需求理解 + 多轮对话**：用户一句话进来，能继续澄清和调整。
- **F2 实时天气（工具）**：涉及户外时自动查天气，并据此调整——比如查到下雨就把当天改成室内为主。
- **F3 景点检索（工具 + 数据）**：能按"亲子 / 公园 / 预算"筛，返回名称、简介、大概花费。
- **F4 攻略知识增强（RAG）**：把在地实用贴士（带伞、交通卡怎么买、别一天排太满）揉进建议里，并且能说清这条来自哪篇攻略。
- **F5 偏好记忆**：用户透露的偏好写进记忆，之后自动规避（说过不爱博物馆，下次就不推）。
- **F6 结构化行程 + 理由**：输出按天的行程，每条都附一句"为什么这么排"。

举个验收标准的例子（F2）：当用户提到某城市、且行程含户外活动时，系统必须调用天气工具，并让"雨天转室内"的调整体现在最终建议里——不能只查不用。

**P1——增强**：行程做成时间轴 + 地图；补平价餐饮和交通建议；行程支持手动改。
**P2——闭环与规模化**：接真实机票/酒店/门票的查询和预订；把记忆升级成结构化用户画像做个性化；扩城市、加多语言。

## 6. 核心流程

```
用户说需求
  → 应用先加载[记忆] + [检索攻略(RAG)]        ← 应用自动做，模型看不见这一步
  → 大模型结合 需求+记忆+攻略 做判断
  → 模型主动请求调用工具（天气 / 景点 / 记忆）   ← 模型驱动，按需触发
  → 应用真正执行工具，把结果回传给模型
  → 模型生成"带理由的分日行程"
  → 用户追问、微调（回到多轮）
```

这里有个我特意想清楚的设计点：**攻略走 RAG（应用每轮自动喂），景点和天气走工具（模型按需要主动调）**。因为攻略这类知识相对稳定、适合预先检索；而天气、价格这种一直在变的，只能实时取。

## 7. 数据与指标

- **北极星指标**：规划完成率——用户从提问到最终接受一份行程的比例。这一个指标最能代表"产品到底有没有帮上忙"。
- **关键指标**（按 AARRR 拆）：首次对话完成率（激活）；平均对话轮数、工具调用成功率（参与）；行程采纳率、建议是否被用户认可（价值）；7 日回访率（留存）。
- **怎么采**：每轮记录调了哪些工具、参数是什么、建议有没有被采纳、用户追问了几次。

## 8. AI 产品特有的考量（这部分是重点）

普通产品的 PRD 到上一节就差不多了，但 AI 产品必须多想几件事：

- **能力边界与降级**：变化极快的数据（价格、余量）一定走工具实时取，绝不放进 RAG——放了也马上过期；模型本身不能执行动作，"帮我订""发邮件"这类只能交给工具。
- **反幻觉**：开放时间、价格这些关键事实以工具/RAG 的数据为准，明确要求模型"没有数据就别编"，并用一组评测样例（eval）做回归，防止改一处坏一片。
- **不把命绑在一家模型上**：模型调用统一走 OpenAI 兼容接口做抽象，换厂商只改配置。这一点我踩过真坑——开发时原本用的免费模型服务中途宣布退役（接口直接返回 410），因为提前做了抽象层，我切到另一家只改了配置、代码一行没动，几分钟就恢复了。这让我更确信 AI 产品要有厂商抽象和降级预案。
- **成本**：优先用小模型加提示词缓存，并监控每次会话的 token 花费。
- **隐私**：用户偏好本地/加密存储，能不收集的就不收集。

## 9. 里程碑

- **M0 · MVP（已完成）**：P0 全部功能，单城市，命令行/网页原型跑通。
- **M1**：结构化行程 + 地图，覆盖 3–5 个城市，做一轮内测把上面的指标跑出来。
- **M2**：接入预订闭环，上结构化用户画像，扩规模与多语言。

## 10. 风险与应对

- 模型能力和价格会变、厂商可能停服 → 多厂商抽象 + 成本监控（已验证可行）。
- 景点/攻略数据会过期 → 做内容更新机制，并让检索结果能溯源。
- 幻觉给出错误建议 → 关键事实以工具数据为准，配 eval 回归。

---

*我的角色：从需求定义、架构设计（工具调用 + RAG + 记忆），到开发和验证，独立完成。*
